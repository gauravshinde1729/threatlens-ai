Stage all changes, create a conventional commit, and push to origin:

1. Run git add -A
2. Look at the staged changes and generate a conventional commit 
   message (feat:, fix:, docs:, test:, ci:) that summarizes 
   what changed
3. Run git commit with that message
4. Run git push origin main

Show me the commit message before executing.
```
